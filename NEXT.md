Next Steps

- Add a module-level page so reviews can be contextualized at the module scope (not just per note group).
- Show a small dot badge on each note group in the sidebar dropdown indicating how many question cards are due.
- Replace the topic chip toggles with a dropdown search + toggle menu to handle large chip lists.
